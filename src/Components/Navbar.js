const Navbar = () => {
  return (
    <div
      style={{
        height: "70px",
        width: "100%",
      }}
    >
      {/* TODO: Build the navbar here */}
    </div>
  );
};

export default Navbar;
